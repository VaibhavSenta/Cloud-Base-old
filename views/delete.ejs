<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Account - Cloude Base</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;

            box-sizing: border-box;
            list-style: none;

            text-decoration: none;
        }
        button a{
            text-decoration: none;
            color: white;
        }
        body{
            display: flex;
            flex-direction: column;
            align-items: center;

            padding: 4vw;
        }

        body img{
            width: 150px;
            height: max-content;
            background-color: black;

            border-radius: 100vw;

            padding: 20px;
            object-fit: contain;
        }
        main {
            display: flex;
            flex-direction: row;
            align-items: center;
            justify-content: center;
            flex-wrap: wrap;

            background-color: gray;

            border-radius: 70px;

            min-width: 200px;
            width: auto;
            min-height: 100px;
            height: auto;
            padding: 10px;
            
        }

        section {
            display: flex;
            flex-direction: column;
            justify-content: flex-start;

            padding: 20px 0px;

            height: 200px;
            min-width: 300px;
            width: auto;

            background-color: transparent;

            margin-left: 20px;
        }
        .btns {
            background-color: gray;
            min-width: 300px;
            padding: 30px;

            margin-top: 10px;
            border-radius: 70px;
        }

        .logout{

            width: auto;
            padding: 6px;
            border-radius: 50px;

            margin-top: 25px;

            outline: none;
            border: none;

            background-color: rgba(0, 0, 0, 0.347);
            color: white;
            cursor: pointer;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;

            background-color: rgba(68, 68, 68, 0.42);

            box-shadow: 30px 40px 50px -20px rgba(0, 0, 0, 0.714);
            
            min-width: 30vw;
            min-height: 20px;

            padding: 20px 0px;

            border-radius: inherit;

        }

        input[type="password"]{
            background-color: transparent;

            box-shadow: -4px 4px 5px rgba(0, 0, 0, 0.268);

            border: none;
            outline: none;

            padding: 5px;

            min-width: 80%;
            color: white;

            border-bottom: 1px solid white;
            border-left: 2px solid white;
        }
    </style>
    <% if (vercelSpeedInsights) { %>
    <!-- Vercel Speed Insights -->
    <script>
    (function() {
        // Initialize Vercel Speed Insights for page performance monitoring
        // This script runs on the client side only
        import('@vercel/speed-insights').then(({ injectSpeedInsights }) => {
            injectSpeedInsights();
        }).catch(err => {
            // Speed Insights failed to load - this is non-critical
            console.debug('Speed Insights initialization failed:', err);
        });
    })();
    </script>
    <% } %>

</head>
<body>
    
    <h1 style="color: brown;">Delete Profile</h1>

    <main>
        <img src="<%= locals.profilePic %>" alt="WDQW">

        <section>

            
            <p><b>Username</b>: <%- locals.userName %></p>
            <p><b>First name</b>: <%- locals.firstName %></p>
            <p><b>last name</b>: <%- locals.lastName %></p>
            <p><b>Email</b>: <%- locals.email %></p>
            <p><b>Date of Birth</b>: <%- locals.dob %></p>
            
        </section>
    </main>
    
    <div class="btns">
        <form action="/<%= locals.userName %>/delete" method="post">
        
            <input type="hidden" name="email" value="<%- locals.email %>" required>

            <label for="password">Confirm password </label>
            <input type="password" name="password" required>
            
            <button class="logout" type="submit" style="color: brown; font-weight: 600; font-size: large;" >Delete Account</button>
        </form>
    </div>
</body>
</html>