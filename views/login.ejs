<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Cloude Base</title>
    <style>
        * {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            box-sizing: border-box;
            list-style: none;

            margin: 0;
            padding: 0;

            color: white;

            text-decoration: none;
            box-sizing: border-box;

            transition: all 0.2s ease; 
        }
        
        body {
            /* background-color: black; */
            background-image: url(../public/backgrounds/loginback.jpg);
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
            backdrop-filter: blur(20px) contrast(180%) brightness(60%);

        }
        
        main {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background-color: transparent;

            width: 100%;
            height: 100vh;

            position: relative;
        }

        main header {
            background-color: transparent;
            width: 100%;
            height: 100%;

            position: absolute;

            padding: 20px;
        }

        main form{
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            box-sizing: border-box;
            
            background-color: rgba(39, 39, 39, 0.824);
            padding: 45px 45px 60px 45px;
            border-radius: 20px;

            box-shadow: 70px 70px 100px -40px black;

            z-index: 1;

            width: 400px;
            min-height: max-content;
            height: 600px;

            min-width: max-content;

            border: 1px solid rgba(255, 255, 255, 0.018);
        }

        form input {
            display: flex;
            flex-direction: column;
            align-items: center;

            background-color: transparent;

            outline: none;
            border: none;
            
            border-bottom: 1px solid rgba(255, 255, 255, 0.504);
           
            width: 100%;

            margin: 20px 0px;
            padding: 5px 5px;

        }
        form h2 {
            width: 100%;
            text-align: center;
            font-size: 30px;
        }

        .innerForm{
            display: flex;
            flex-direction: column;

            background-color: transparent;
        }

        .resetpassword{
            
            font-size: 15px;
            opacity: 0.94;
            
        }
        .submit{
            width: 100%;
            height: auto;

            display: flex;
            align-items: center;
            flex-direction: column;
        }

        button{
            background-color: rgb(91, 29, 185);
            color: white;

            outline: none;
            border: none;

            padding: 10px 50px;

            width: 100%;

            border-radius: 5px;

            margin-top: 60px;

            cursor: pointer;
            &:hover{
                transform: scale(1.03);
                background-color: rgb(79, 27, 158);
            }
            &:active{
                background-color: rgb(108, 71, 187);
            }
        }

        form p{
            font-size: 14px;
            opacity: 0.97;
            width: 100%;
            text-align: end;
        }
        form .createAcc{
            font-size: 14px;
            cursor: pointer;
            color: rgba(255, 172, 84, 0.845);

            text-decoration: underline;
        }

    </style>
    <!-- Vercel Speed Insights -->
    <% if (vercelSpeedInsights) { %>
    <!-- Vercel Speed Insights -->
    <script>
    (function() {
        // Initialize Vercel Speed Insights for page performance monitoring
        // This script runs on the client side only
        import('@vercel/speed-insights').then(({ injectSpeedInsights }) => {
            injectSpeedInsights();
        }).catch(err => {
            // Speed Insights failed to load - this is non-critical
            console.debug('Speed Insights initialization failed:', err);
        });
    })();
    </script>
    <% } %>
</head>
<body>
    



    <main>

        <header>

            <h1>Wellcome</h1>
        </header>
    
        <form action="/login" method="post" >
    
            <h2>LOGIN</h2>
            <div class="innerForm">

                <!-- <label for="email">Enter email</label> -->
                <input type="email" name="email" id="email" placeholder="Enter Email" required>
    
                <!-- <label for="password">Enter password</label> -->
                <input type="password" name="password" id="password" placeholder="Enter Password" required>
                
                <a class="resetpassword" href="/resetpassword">Didn't remember password ??</a>
                <br>
        
                <br>
                <p>No Account   ?</p>
                
                <a class="createAcc" href="/signup">Create Account</a>
                
            </div>
            <div class="submit">

                <button type="submit">Login</button>
            </div>
        </form>
    </main>
</body>
</html>