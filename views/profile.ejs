<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile - Cloude Base</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;

            box-sizing: border-box;
            list-style: none;

            text-decoration: none;
        }
        button a{
            text-decoration: none;
            color: white;
        }
        body{
            display: flex;
            flex-direction: column;
            align-items: center;
            background-image: url(../public/backgrounds/background.jpg);
            background-position: center;
            background-size: cover;
            background-repeat: no-repeat;
            
            
            padding: 4vw;
        }

        body img{
            width: 150px;
            height: max-content;
            background-color: black;

            border-radius: 100vw;

            padding: 20px;
            object-fit: contain;
        }
        main {
            display: flex;
            flex-direction: row;
            align-items: center;
            justify-content: center;
            flex-wrap: wrap;

            background-color: rgba(32, 32, 32, 0.258);
            backdrop-filter: blur(10px);
            box-shadow: 40px 40px 50px -30px rgba(0, 0, 0, 0.796);


            border-radius: 30px;

            min-width: 200px;
            width: auto;
            min-height: 100px;
            height: auto;
            padding: 10px;
            
        }

        section {
            display: flex;
            flex-direction: column;
            justify-content: flex-start;

            padding: 20px 0px;

            height: 200px;
            min-width: 300px;
            width: auto;
            background-color: transparent;

            margin-left: 20px;
        }
        .btns {

           
            background-color: rgba(32, 32, 32, 0.258);
            backdrop-filter: blur(10px);
            box-shadow: 40px 40px 50px -30px rgba(0, 0, 0, 0.796);

            min-width: 300px;
            padding: 30px;

            margin-top: 10px;
            border-radius: 30px;
        }

        .logout{

            width: 130px;
            padding: 6px;
            border-radius: 5px;

            outline: none;
            border: none;

            background-color: rgba(0, 0, 0, 0.666);
            color: white;
            cursor: pointer;
        }
    </style>
    <!-- Vercel Speed Insights -->
    <% if (vercelSpeedInsights) { %>
    <!-- Vercel Speed Insights -->
    <script>
    (function() {
        // Initialize Vercel Speed Insights for page performance monitoring
        // This script runs on the client side only
        import('@vercel/speed-insights').then(({ injectSpeedInsights }) => {
            injectSpeedInsights();
        }).catch(err => {
            // Speed Insights failed to load - this is non-critical
            console.debug('Speed Insights initialization failed:', err);
        });
    })();
    </script>
    <% } %>
</head>
<body>
    
    
    <h1>Profile</h1>

    <main>
        <img src="<%= locals.profilePic %>" alt="WDQW">
    
        <section>

            <p><b>Username</b>: <%- locals.userName %></p>
            <p><b>First name</b>: <%- locals.firstName %></p>
            <p><b>last name</b>: <%- locals.lastName %></p>
        </section>
    </main>
    
    <div class="btns">
        <button class="logout"><a href="/<%= locals.userName %>/logout">Logout</a></button>
        <button class="logout"><a href="/<%= locals.userName %>/delete">Delete Account</a></button>
        <button class="logout"><a href="/<%= locals.userName %>/manage">Manage Account</a></button>
    </div>
    

</body>
</html>