<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Cloude Base</title>
    <style>
        * {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            box-sizing: border-box;
            list-style: none;
            text-decoration: none;

            margin: 0;
            padding: 0;

            color: rgba(255, 255, 255, 0.899);

            transition: all 0.3s ease-in-out;
            cursor: default;
        }

        ::-webkit-scrollbar{
            background-color: transparent;
            width: 8px;
        }
        ::-webkit-scrollbar-track{
            background-color: transparent;
        }

        ::-webkit-scrollbar-thumb{
            background-color: rgba(76, 76, 76, 0.62);
            border-radius: 50px;
        }
        a,button {
            cursor: pointer;

            outline: none;
            border: none;
        }
        
        
        /* body {
            display: flex;
            align-items: center;
            flex-direction: column;
            justify-content: flex-start;

            position: relative;

            padding: 2vh 1vw;

            background-color: black;
        }  */

        nav{
            display: flex;
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
            

            position: sticky;
            z-index: 1;

            background-color: inherit;
            width: 100vw;

            top: 0px;

            padding: 16px 8px;

            border-radius: 10px;

        }

        .profileLogo{
            display: flex;
            flex-direction: row;

            background-color: transparent;

            height: 100%;
            width: auto;

        }
        .profileLogo button {
            background-color: slateblue;

            color: white;
            
            padding: 10px;

            margin-right: 30px;

            border-radius: 5px;

            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.567);
            outline: none;
            border: none;

        }
        .profileLogo button a {
            color: inherit;
            text-decoration: none;
            font-size: 1rem;
        }

        nav> h1 {
            font-size: 30px;
        }

        body>:nth-child(2){


            /* margin-top: 14vh; */
        }















































        body{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;

            background-color: black;

            overflow: hidden;

            width: 100vw;
            height: 100vh;
        }


        main{
            display: flex;
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
            
            background-color: transparent;
            
            width: 100%;
            height: 100vh;

        }
        
        .panel{
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            justify-content: flex-start;

            position: relative;

            background-color: rgb(27, 27, 27);
            
            width: 70px;
            height: 100%;

            padding: 20px 0px;

            border-radius: 10px;
            perspective: 1000px;
            
            overflow: hidden;
        }
        .panel .btn1{
            display: flex;
            align-items: center;
            justify-content: center;
            flex-wrap: wrap;
            text-wrap: wrap;

            background-color: rgb(100, 100, 100);
            box-shadow: 0px 10px 10px rgba(0, 0, 0, 0.623);

            max-width: 100%;
            height: auto;
            
            padding: 25px 5px;
            margin: 5px 0px;

            font-size: 13px;
            font-weight: 700;
        }
        .center{
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;

            background-color: rgb(27, 27, 27);

            border-radius: 10px;

            width: 100%;
            height: 100%;

            margin: 5px;
            padding: 10px;

            overflow-y: auto;
        }

        .center section{
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;
            align-items: center;
            justify-content: space-evenly;

            background-color: transparent;
            width: 100%;
        }
        














































        section {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            flex-direction: column;

        }
        section header{
            width: 100%;
            font-size: 30px;
            font-weight: 800;
        }
     
        

        .movieCard{
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
            align-items: center;

            position: relative;
            background-color: rgb(44, 44, 44);

            border: 2px solid transparent;
            border-radius: 15px;

            width: max-content;
            height: auto;

            /* aspect-ratio: 2/3; */

            margin: 10px;
            padding: 10px;

            overflow: hidden;
            box-shadow: 20px 20px 30px -5px rgba(0, 0, 0, 0.427);

            &:hover{
                box-shadow: 40px 40px 40px -20px rgba(0, 0, 0, 0.87);
                border-color: rgba(219, 219, 219, 0.732);
                transform: scale(1.01);
                cursor: pointer;
            }
        }

        .movieCard img{

            width: inherit;
            aspect-ratio: inherit;
            object-fit: cover;
            position: relative;
        }

        .text{
            display: flex;
            flex-direction: column;
            flex-wrap: wrap;
            align-items: center;
            text-align: center;
            
            position: absolute;
            background: linear-gradient(to top, rgb(0, 0, 0)10%, rgba(253, 253, 253, 0)90%);

            width: 100%;
            height: 100%;

            padding: 6px;

            text-shadow: 2px 1px 3px black;

            &:hover{
                cursor: pointer;
            }
        }
        .keyValue{
            display: flex;
            align-items: flex-start;
            justify-content: flex-start;

            background-color: transparent;

            width: 100%;
            height: auto;

            font-size: 18px;
        }
        .keyName{
            color: rgb(237, 237, 237);
            font-weight: 600;
            font-size: 20px;
        }
        .value{
            color: rgb(148, 77, 10);
            font-weight: 500;
            font-size: 18px;
        }
        
        .movieCard{
            display: flex;
            flex-direction: column-reverse;
            align-items: center;

            position: relative;
            background-color: transparent;

            border: 2px solid transparent;
            border-radius: 15px;

            width: 200px;

            aspect-ratio: 2/3;

            margin: 14px;
            padding: 0px;

            overflow: hidden;
            box-shadow: 20px 20px 30px -5px rgba(0, 0, 0, 0.427);

            &:hover{
                box-shadow: 40px 40px 40px -20px rgba(0, 0, 0, 0.87);
                border-color: rgba(219, 219, 219, 0.732);
                transform: scale(1.01);
                cursor: pointer;
            }
        }

        .movieCard img{

            width: inherit;
            /* aspect-ratio: inherit; */
            object-fit: cover;
            position: relative;
        }

        .text{
            display: flex;
            flex-direction: column-reverse;
            flex-wrap: wrap;
            align-items: center;
            text-align: center;
            
            position: absolute;
            background: linear-gradient(to top, rgb(0, 0, 0)10%, rgba(253, 253, 253, 0)90%);

            width: 100%;
            height: 100%;

            padding: 6px;

            text-shadow: 2px 1px 3px black;

            &:hover{
                cursor: pointer;
            }
        }
        .movieCard a{
            position: absolute;
            bottom: 0;
            width: 100%;
            height: 100%;
            z-index: 3;

            background-color: transparent;

        }


        
    </style>
    <% if (vercelSpeedInsights) { %>
    <!-- Vercel Speed Insights -->
    <script>
    (function() {
        // Initialize Vercel Speed Insights for page performance monitoring
        // This script runs on the client side only
        import('@vercel/speed-insights').then(({ injectSpeedInsights }) => {
            injectSpeedInsights();
        }).catch(err => {
            // Speed Insights failed to load - this is non-critical
            console.debug('Speed Insights initialization failed:', err);
        });
    })();
    </script>
    <% } %>

</head>
<body>
    
    <nav>

        <h1>Cloude Base Admin</h1>
    
        <div class="profileLogo">

            <button>
                
                <a href="/admin/logout"> Logout </a>
                <!-- <a href="">vaibhav999</a> -->
            </button>

            <button>
                
                <a href="#"> <%= adminName %> </a>
                <!-- <a href="">vaibhav999</a> -->
            </button>
            
        </div>

    </nav>

    <main>
        <div class="panel">
            <!-- <div class="btn1">Hollywood</div>
            <div class="btn1">Bollywood</div> -->

            
            
        </div>
        <div class="center">

            
            <section class="section1">
                
                <header>All Movie details</header>

                <% movies.forEach(movie => { %>
                    
                    <div class="movieCard">
                        <img src="<%= movie.poster %>" alt="movieCard">
                        <div class="text">
                            <p><%= movie.details %></p>
                            <h4><%= movie.titleName %></h4>
    
                        </div>
                        <a  href="/admin/movies/<%= movie.ucbid %>"></a>
                    </div>

                <% }) %>


                
                
            </section>
        </div>
    </main>

    
    
</body>
</html>